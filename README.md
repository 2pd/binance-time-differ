# Binance Server Time Differ

A small tool to send a request to `https://api.binance.com/api/v3/time`, and print out the difference with local time.


```shell

go build differ.go

./differ

# with logs
./differ -v

```
